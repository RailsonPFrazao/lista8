#!/bin/bash

echo "No case usamos o 'esac' para delimitar o final do case assim como usamos o 'fi' para o if ou. Tmbém encontramos o '*)' que é utilizado para quando os testes não atendem nenhum dos requisitos de testados então ele realiza a tarefe após o '*)'."
